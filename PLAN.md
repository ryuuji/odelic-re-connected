
# このプロジェクトの目標

- ODELICのConnected Lighting製品のアプリを再開発する
- BLE通信を使用して、スマートフォンから照明器具を制御する
- 既存のアプリを解析して、不安定な接続の原因を特定する
- 通信プロトコルを解明して、高速に動作するAndroidアプリを開発する

# 参考情報

これまでの調査内容（関連しそうな内容）
- https://www.pairlink.com.cn/pl-web/product/ble-spec
- ⭐ **ベンダー公式仕様書あり** https://ito-elec.jp/ble/ → [02 C26](docs/02-protocol.md)
  - `PLTBEITO_独自メッシュ_UARTコマンド仕様書_V1.2.pdf` =
    **Multilink UART Protocol**（Suzhou Pairlink Network Technology）。
    PDU の `宛先 vAddr(4) + チャネル(1) + データ` 構造と
    **チャネル 0〜31**（電波上は `0x20 | ch`）が明記されていた
  - データシート: **ARM Cortex-M4 / Bluetooth 5 / 外付け 8Mbit SPI Flash**
    （Cypress CYW207xx 系）
  - ⚠️ HOMEID・パスワード・暗号化・照明コマンドは**書かれていない**
- https://www.odelic.co.jp/products/connectedlighting/

## 現在の識別子（これが唯一の正）

| 項目 | 値 | バイト列 |
| --- | --- | --- |
| アプリの ID 表示 | `12345678` | — |
| HOMEID（上位 4 桁） | `1234` | `D2 04 00 00`（10 進 1234 = 0x04D2 の LE） |
| パスワード（下位 4 桁） | `5678` | `35 36 37 38`（ASCII） |

⚠️⚠️ **上の値はプレースホルダ（ダミー）です。**下位 4 桁はメッシュのパスワードで、
近隣の誰でも照明を操作できてしまうため、リポジトリには実値を書きません。

実値は**環境変数で渡します**。ソースにも設定ファイルにも埋めないでください。

```bash
export ODELIC_ID=<実際の 8 桁 ID>          # 解析ツール（decrypt_recv.py 等）
export ODELIC_HOMEID=<HOMEID の 10 進>     # adv_raw.sh
sudo ./install.sh <実際の 8 桁 ID> 8080    # odelicd は /etc/default/odelicd (0600) に保存
```

調査で追加された情報 → [docs/01-findings.md](docs/01-findings.md)

# フェーズ

| # | フェーズ | 状態 | 詳細 |
| --- | --- | --- | --- |
| 0 | 環境構築（adb / jadx / btsnoop パーサ） | ✅ 完了 | [04](docs/04-analysis-procedure.md) |
| 1 | 既存アプリの APK 取得 | ✅ 完了 | [04](docs/04-analysis-procedure.md) |
| 2 | 静的解析（逆コンパイル） | ✅ 完了 | [02](docs/02-protocol.md) |
| 3 | 動的解析（HCI スヌープログ） | ✅ 実施・検証完了 | [02 C18](docs/02-protocol.md) |
| 4 | プロトコルの文書化 | ✅ **完了**（送受信の暗号まで） | [02](docs/02-protocol.md) |
| 5 | **Raspberry Pi 版の実用化** | ✅ **完了・常駐運用中** | [06](docs/06-raspberrypi-setup.md) |
| 5b | **状態取得（受信復号）** | ✅ **完了** | [02 C23](docs/02-protocol.md) / [06 P6](docs/06-raspberrypi-setup.md) |
| 5c | **Matter 対応（Google Home から操作）** | ✅ **完了・稼働中** | [07](docs/07-matter.md) |
| 5d | **設定ページとスマホ UI** | 🚧 **作りかけ**（共有パッケージと HTTPS 証明書まで） | [08](docs/08-web-ui.md) / [09 引き継ぎ](docs/09-handoff-web-ui.md) |
| 6 | 新アプリの実装（Android） | 未着手 | [05](docs/05-app-design.md) |
| 7 | 破壊的操作の解析（グループ設定・登録） | 未着手 | [04](docs/04-analysis-procedure.md) |

## スコープ外（今回は作らない）

| 機能 | 理由 |
| --- | --- |
| **器具の登録・プロビジョニング**（ECDH / `SET_MESH_ENCRY`） | 破壊的。純正アプリに任せる |
| **グループ設定の変更**（`0x30` / `0x31`） | 同上。**読み取りのみ**行う |
| **HOMEID の発行・変更** | 破壊的（ローカルデータ削除 + メッシュ退出を伴う） |
| タイマー / スケジュール | 中心機能に絞るため |
| シーン（`0x40`〜`0x42`） / センサー連携 | 同上 |

⚠️ **これらの送信コマンドは新アプリに実装しない**（誤送信の余地を残さない）。
プロトコル上の手がかりは [02 C7 / C15-5 / C15-10](docs/02-protocol.md) に残してある。

## ⭐ 方式: HOMEID を純正アプリから引き継ぐ

**器具の登録は純正アプリで済ませ、新アプリは 8 桁 ID を教えてもらって参加するだけ。**
→ 詳細は [05 の「HOMEID の引き継ぎ方式」](docs/05-app-design.md)

- 純正アプリの**メニュー画面に `ID:12345678` と表示されている**
  （`textView.setText("ID:" + Settings.HOMEID + Settings.PASSWORD)`）
- 分解は `changeID()` と同一: 上位 4 桁を 10 進 → LE 4 バイト、下位 4 桁は ASCII
- ⭐ **入力ミスは即座に検出できる。** 器具が送ってくる `PERIPHERAL_LOGIN` を
  LOGINKEY で復号し、先頭 4 バイトが HOMEID と一致するかを見るだけ（C23-1）
- 器具一覧・vAddr・**グループ所属**・現在状態は**すべて自動で読み取れる**
- 引き継げないのは**器具の表示名・配置**だけ（純正アプリのローカル DB のみ）
- 純正アプリと**同時に使える**（C28-3 で実証）

不安定さの核心（**分割 PDU の欠陥**）は [02 C30](docs/02-protocol.md) /
[03 I7](docs/03-instability.md) で完全に特定し、受信側は `odelicd` で修正済み。

# 判明したこと（2026-07-25 静的解析）

純正アプリには **Pairlink「Connected Mesh」SDK が難読化されずに丸ごと含まれていた**。
プロトコルはほぼ読み切れた。

1. **方式は GATT ベースのメッシュ**。スマホが Central または **Peripheral（GATT サーバ）**
   として 1 台の器具と接続し、そこを入口にメッシュ全体へコマンドを流す。
   → 「ペリフェラルモード対応機種が必須」の正体はこれ（アドバタイズのためではなかった）
2. **照明制御コマンドを完全に解読**。明るさ 0xC0/0xC1、調色 0xE2/0xE3、
   グループ 0x30/0x31、シーン 0x40〜0x42。ペイロードの各バイトの意味まで確定
   - `0xC0` は**サブコマンド方式**（0=明るさ+色温度 / 1=常夜灯 / 2=スポット / 3=サイド RGB）
   - **ON = 55 / OFF = 50** という特殊コード。それ以外はテーブル引きのコード値
   - **状態応答も解読済み**。`0x70` で要求 → `0x71` で応答。
     ⭐ 明るさコードは**逆順**（0 が最も明るい）
   - **コマンドはすべて絶対値指定** → 再送しても二重に効かない（冪等）
3. **PDU フォーマット確定**。メッセージデータは最大 9 バイト。
   一斉操作はチャネル 0x2A、個別・グループは 0x20
4. **HOMEID とパスワード** — ⭐ **アプリの ID 表示 8 桁 = HOMEID 4 桁 + パスワード 4 桁**
   - HOMEID は 10 進数のリトルエンディアン 4 バイト、パスワードは ASCII 4 バイト
   - 現在の ID `12345678` → HOMEID `D2 04 00 00` / パスワード `35 36 37 38`
   - **実機ログで両方が平文で流れていることを確認**（`02 00 D2 04 00 00 35 36 37 38`）
5. **不安定さの原因を 1 つコードで確定**。セグメント分割の再組み立てが
   欠落を検知せず、状態もリセットせず、タイムアウトも持たない
   → グループ設定の保存失敗と、その後の操作不能・初期化必要の説明になる
6. **器具のファームウェアが APK に同梱**（`assets/ota/*.mp3` は音声ではない）

# 実機 HCI ログで検証（2026-07-25）→ [docs/02-protocol.md](docs/02-protocol.md) C18

静的解析の結論はほぼ全面的に裏付けられ、**2 つの重要な訂正**が判明した。

1. ⭐⭐ **PDU は平文で流れていた。** 照明制御（`DATA_EVENT`）は暗号化されていない。
   暗号化されるのは PDU タイプ `0x06` とログイン（`0x19`）のみ。
   → **`.so` を使わない完全自前実装（案 D）が視野に入った**
2. ⭐ **スマホは GATT サーバ（Peripheral）で動作していた。**
   器具がスマホに接続してきて、コマンドは `Handle Value Notification` で送る
3. ON=`0x37`(55) / OFF=`0x32`(50) を実測確認。**予測と完全一致**
4. **再送していないことを実証**（各操作 1 回だけ）→ I1 確定
5. **パスワードが平文で流れる**（`02 00 D2 04 00 00 35 36 37 38`）
6. **`PLTCEOC-05` は器具が GATT で公開する型番**（問い合わせ不要になった）
7. 訂正: アドバタイズは数百 ms ではなく **21〜95 秒** 出しっぱなしだった

→ プロトコル詳細: [docs/02-protocol.md](docs/02-protocol.md)
→ 不安定さの原因: [docs/03-instability.md](docs/03-instability.md)
→ 実装方針: [docs/05-app-design.md](docs/05-app-design.md)

# TODO

- [x] 既存アプリの解析
- [x] 各 MSGID のメッセージデータの中身を特定する
- [x] HOMEID / パスワードと `setHomeidPassword` の対応を特定する
- [x] 実機 HCI ログで静的解析の結論を電波上で確認する
- [x] `PLTCEOC-05` の正体を確認する（問い合わせ不要になった）

# ⭐⭐⭐ 自前実装で照明の制御に成功（2026-07-25）

**純正アプリも `libnative-lib.so` も Pairlink SDK も使わず、
Raspberry Pi から照明を点灯・消灯できた。**（目視確認済み）

```
点灯: 03 FF FF FF FF 20 25 00 00 00 C1 37 37 00 00 00 00 00 00 00
消灯: 03 FF FF FF FF 20 25 00 00 00 C1 32 32 00 00 00 00 00 00 00
```

純正アプリの実機ログと **src の vAddr 以外は完全に同一**。
**プロトコル解析はここで完了。**

→ 詳細は [docs/02-protocol.md](docs/02-protocol.md) C19、
実施記録は [docs/06-raspberrypi-setup.md](docs/06-raspberrypi-setup.md)

**当時の最大の発見: `PERIPHERAL_LOGIN`（`01 19` + 16 バイト）には応答してはいけない。**

| `0x19` への対応 | 結果 |
| --- | --- |
| エコーバックで応答 | ❌ 12 回すべて切断された |
| **応答しない** | ✅ **WELCOME → 参加成功** |

⚠️ **これは誤りだった（→ C23-2 で訂正）。** 切断されていたのは
「エコーバックという誤答」だったからで、**正しい応答は計算できる**。
正しく応答すると器具が状態も返してくれる。

`GET_PASSWORD` に平文パスワードを返すだけでも参加はできる（暗号処理は不要）。
→ **案 D（完全自前実装・再配布可能）で確定。**

その他の実測。

- 器具 2 台（`EC:C5:7F:81:DE:CD` / `EC:C5:7F:80:28:A6`）が Pi に接続
- HOMEID `D2 04 00 00` を**器具側が送ってきた** → C16 の解釈が完全に正しい
- own_vAddr は**器具から割り当てられる**（実行ごとに変わる）
- **器具は普段アドバタイズしない。**「スキャンする側」なので受動スキャンでは発見不可
- ⚠️ ~~**器具はコントローラのアドレスを記憶している。**
  同じアドレスでは再接続してこなくなり、ランダムアドレスに変えたら 1.9 秒で復活~~
  → **誤りだった（C31 / C32 で訂正）。** 真因は
  **BlueZ がプロセス終了後も ACL リンクを保持していたこと**。
  古いリンクを切れば**同じアドレスでも 5 秒で再接続してくる**
- ⭐ **接続は数秒で切れる。** 参加を検知したら即座に送る必要がある
- BlueZ の D-Bus アドバタイズは Pi 3 では使用不可（`SupportedInstances = 0`）
  → raw HCI（`hcitool`）で回避

# ✅ Raspberry Pi 版を実用化（2026-07-25）

`odelicd` を systemd で常駐させ、HTTP API で操作できるようにした。
→ [docs/06-raspberrypi-setup.md](docs/06-raspberrypi-setup.md) の P5

| | 純正アプリ | odelicd |
| --- | --- | --- |
| 起動〜操作可能 | 約 7 秒 | **0 秒**（常時接続維持） |
| 1 操作の所要時間 | 不明 | **5〜8 ミリ秒** |
| 効いたことの確認 | しない | **277〜320 ms で収束確認**（`?wait=1`） |
| 取りこぼし対策 | 送信 1 回のみ | **1 通 + 状態応答で確認して再送**（到達率 0.993〜1.000） |
| リンク寿命 | — | **6.8 分以上・切断 0**（旧実装は 3 分で 22 回参加し直していた） |

→ 通信戦略の実測と最適化は [02 C33](docs/02-protocol.md)。

当時は**接続維持の鍵は `SET_LINK` (`01 10`)** だと考えていた。純正アプリのログと比べて
唯一送っていなかったコマンドで、送るようにしたら数秒 → 90 秒以上に伸びたため。

⚠️ **これも誤りだった（→ C23-6 で訂正）。** 切断の真因は
`PERIPHERAL_LOGIN` に応答していなかったこと。`SET_LINK` は
**2 台目以降のバックアップリンク専用**で、送ると器具が応答を返さなくなる。

# 次の焦点：Android アプリの実装

プロトコルは解明・実証が完了した。→ [05](docs/05-app-design.md) の手順に従う。

Pi での実証で判明した**設計上の必須事項**を反映する。

- [x] ⭐ **アドバタイズとアドレスの問題は「存在しなかった」**（C31 / C32）
      - 器具は**広告アドレスも AD 識別子も記憶していない**（3 条件の実験で切り分け）
      - 再接続を阻んでいたのは **古い ACL リンク**。切れば同じアドレスで 5 秒で繋がる
      - → **Android が広告アドレスを制御できないことは問題にならない**。
        必要なのは「切断を検知したら確実にアドバタイズを再開する」ことだけ
      - AD には**永続化した疑似 MAC**（アプリ層の identity）を入れる。
        純正アプリは `SharedPreferences` に保存している（`BleUtil.getBTMac`）
      - 設定は LOW_LATENCY（実測 100〜131 ms）/ TX HIGH / 接続可 / タイムアウトなし
- [ ] 切断時は**残っている接続を確実に片付けてから**広告を張り直す（C32）
- [x] ⭐⭐⭐ **参加したら接続の受け付けを止める**（C33-3）。これが安定性の核心。
      出し続けると器具が次々に繋いできて、**そのたびにメッシュが古いリンクを切る**
      （新リンク確立の 0.7〜1.4 秒後・完全な交互）。メッシュは
      「コントローラ 1 台につきリンク 1 本」しか許さない。
      ⚠️ Linux では広告を消せない（BlueZ が 150〜240 ms 後に再開する）ので
      `ADV_NONCONN_IND` に切り替える。**Android では素直に止められる**
- [x] ⭐ **リンクは 1 本で足りる。冗長化しない**（C33-3）。
      メッシュが中継するので、繋いでいない器具の状態も RTT 78 ms で返ってくる
- [x] ⭐ **切断の検知は `Device1.Connected`（Android なら `onConnectionStateChange`）で行う。**
      BlueZ の `StopNotify` は当てにならない（24 h で確立 50 回 / 切断 2 回）（C33-8）
- [x] ⭐ **1 操作 = 1 通で足りる**（C33-5）。到達率は 0.993〜1.000。
      3 連射は上りも下りも 3 倍にするだけだった。
      「同じ PDU が二重に届く」現象も**自分の連射が原因**だった
- [x] ⭐ **確認は実測 RTT の p90 × 2 後に投げる**（C33-7）。固定 1500 ms は p90 の 11 倍
- [x] ⭐ **Connection Interval を確認する**（C33-1 / C33-2）。器具は 15.00 / 28.75 ms を
      指定してくる。notify のレイテンシはこれに律速される。
      ⚠️ Linux では BlueZ が 45 ms に書き換えるので `conn_min_interval` を下げる
- [ ] 操作はキューに溜め、接続の瞬間に流す
- [ ] ⭐ **`PERIPHERAL_LOGIN` に正しい応答を返す**（C23-2）。
      これをしないと器具が数秒で切断し、応答も返さない
- [ ] ⭐ **`SET_LINK` (`01 10`) を主リンクに送らない**（C23-6）。
      送ると器具が「バックアップ扱い」にして応答を返さなくなる
- [ ] 参加したら暗号化 Ping → 器具一覧（vAddr）を確保してから個別制御・状態要求
- [ ] 受信 `type 0x06` を復号して状態を反映（鍵はログイン要求から。リンク単位）
- [ ] ⭐ **他コントローラのコマンド（平文 `0xC0`/`0xC1`/`0xC5`）を観測して状態に反映**（C28）。
      重複排除と、確認のための状態要求（デバウンス）まで込みで
- [ ] 送信は `notifyCharacteristicChanged(device, …)` で**器具ごとに送り分け**、
      `onNotificationSent` を待って直列化する（純正アプリと同じ。BlueZ にはできない）
- [ ] 明るさスライダーは中間値を連射しない（純正アプリは 143 ms 間隔で 16 通送っていた）
- [ ] 常駐: フォアグラウンドサービス + `foregroundServiceType="connectedDevice"`（Android 14+）、
      電池最適化の除外、`WAKE_LOCK`、起動時自動開始（純正アプリもこの構成）
- [ ] ⭐ **分割 PDU（タイプ `04 04`）の再組立を自前で正しく実装**（C30）。
      純正アプリの実装は 1 個落ちると**以降の分割転送が二度と完成しない**
- [ ] 主リンクは 1 本に絞るか（純正アプリ方式: 参加後に広告停止 + 余剰は `01 15 55` で切断）、
      複数維持するか（`odelicd` 方式: 冗長だが重複排除が必要）を決める（C29）
- [ ] MTU は 217 が期待値（`real_mtu = MTU - 3 = 214`）。切断時は 20 に戻す（C29-5）
- [ ] 同じコマンドを複数回送る（絶対値指定なので冪等）

## ⭐⭐⭐ 状態取得も実現（2026-07-25）→ [02](docs/02-protocol.md) C23

一度は「暗号化に阻まれた」と判断したが、**受信復号を完全に解明して突破した。**
器具の一覧・vAddr・グループ・ファーム・**現在状態**がすべて読める。

| 項目 | 状況 |
| --- | --- |
| 照明の制御 | ✅ 平文で送れる。実証済み |
| 器具台数 | ✅ `BROADCAST_MESHINFO` から（平文） |
| 接続してきた器具の MAC | ✅ D-Bus のデバイスパス + Ping 応答 |
| 器具の vAddr / 機種 / ファーム | ✅ **暗号化 Ping への応答を復号** |
| 器具のグループ ID | ✅ `0xD7` 応答を復号 |
| **器具の現在状態（ON/OFF・明るさ・色温度）** | ✅ **`0x71` 応答を復号** |

**鍵は器具が接続直後に自分から渡してきていた。**
`PERIPHERAL_LOGIN`（`01 19` + 16 バイト）を LOGINKEY で復号すると
`[0..3]` = HOMEID、`[4..7]` = **そのリンクの XOR ホワイトニング鍵**。
あとは `XOR → AES_ECB_decrypt(EVENTKEY) → PKCS#7 除去` で平文になる。

### ⚠️ 過去の結論を 3 つ訂正した

| 旧結論 | 正しい内容 |
| --- | --- |
| C19-2「`PERIPHERAL_LOGIN` に応答してはいけない」 | **正しい応答を返すのが正解。** 切断されていたのはエコーバックという誤答だったため |
| C19-8「`SET_LINK` を送らないと数秒で切れる」 | **切断の真因はログイン無応答。** `SET_LINK` は 2 台目以降のバックアップリンク専用で、全器具に送ると**器具が応答を返さなくなる** |
| C22-1「送信はゼロパディング・XOR なし」 | **PKCS#7 + AES の後に XOR ホワイトニング**（`sendEncry` が外でやっている） |

### 閉ループを実機で確認

| 指示 | 器具が返した状態 |
| --- | --- |
| `bright=60&color=50` | **`on=true bright=60 color=50`** ✅ |
| `off` | **`on=false`** ✅ |
| `night?level=0` | **`night_level=0`（器具値 3）** ✅ |
| `night?level=1` | **`night_level=1`（器具値 2）** ✅ |

⭐ ナイトライトの状態は `0x71` 応答の `data[7]` にある（C24-5）。
**純正アプリはこのバイトを読んでいないので、自前実装のほうが正確に追える。**

C15-9 の換算式が実機の状態応答で裏付けられた。
→ **P4「確認できるまで成功と言わない」が実装可能になった。**

検証ツール: `python tools/decrypt_recv.py <btsnoopログ> 12345678`

### ⭐⭐⭐ 他コントローラの操作も追従できる（実験 B・→ [02 C28](docs/02-protocol.md)）

器具は状態変化を自発通知しないが、**他のコントローラが送ったコマンドは
平文で中継されてくる**。純正アプリで操作しながら Pi で観測して確認した。

```
<< 03 FF FF FF FF 20 25 00 00 00 C1 37 37 ... 01   点灯（グループ1）
<< C1 00 12 → C1 00 03 を 16 連射                   明るさスライダー
<< 03 FF FF FF FF 20 25 00 00 00 C5 00 01 ... 01   ナイトライト level 1
```

- `src vAddr`（`25 00 00 00` = スマホ）が自分と違えば他人の操作と判る
- ⚠️ 同じ PDU が**二重に届く**（中継経路が複数）→ 重複排除が必要
- ⚠️ 純正アプリは明るさの中間値を**16 通も連射**していた（約 143 ms 間隔）
- ✅ **共存問題なし**。純正アプリが操作中も Pi のリンクは 1768 秒継続

⭐ 観測した `C5 00 01 … 01`（グループ 1・level 1）に対して、器具が
`night=2`（= level 1）を返した。**C24 のナイトライト解読が純正アプリの
実トラフィックで独立に裏付けられた。**

`odelicd` は観測 → 状態反映 → 1.5 秒後に確認の状態要求（P4）まで実装済み。

## 複数デバイスへの対応 ✅

**器具が別グループ（0 と 1）に入っていたため、グループ 0 だけに送ると 1 台しか反応しなかった。**
既定を「全器具一斉」（`MSGID 0xC0` + チャネル `0x2A`）にして解決。

| 対象 | 指定 |
| --- | --- |
| 全器具を一斉に（既定） | `?target=all` |
| グループ単位 | `?target=group:N` |
| 器具を個別に | `?target=dev:<KEY>` ✅ **使えるようになった**（vAddr を Ping 応答から取得・C23-4） |
| ナイトライト | `POST /night?level=0\|1\|2`（上記の target 指定も可・C24） |

# `.so` の静的解析（2026-07-25）→ [docs/02-protocol.md](docs/02-protocol.md) C21

`libnative-lib.so`（arm64）を capstone + lief（`tools/disasm.py`）で解析。
**シンボルが全て残っており、鍵導出を完全に解明した。**

⭐ **鍵 = HOMEID とパスワードを 1 バイトずつ交互に並べ、後半に固定文字列**

```
鍵1（コマンド用）: D2 35 04 36 00 37 00 38 + "LOGINKEY"
鍵2（イベント用）: D2 35 04 36 00 37 00 38 + "EVENTKEY"
                   └ homeid/pwd 交互 ┘   └ 固定 ┘
```

ただし **AES が標準実装ではなかった**。送信 Ping（平文既知）が
標準 AES-128-ECB で再現せず、独自 S-box などの可能性。
→ 完全な復号再現は工数大。**`.so` を Pi で直接 `ctypes` 呼び出しするのが最短**（案 J）。

## 残る要検証

- [x] 状態取得 → ✅ **完全自前実装で実現**（C23）。`.so` の dlopen も Frida も不要だった
- [x] `data[0]` / `data[1]` のどちらが明るさか → **確定**
      - `data[0]` = 色温度（`color% / 5`）、`data[1]` = 明るさ（`(100 - bright%) / 5`、逆順）
      - **コマンドと状態応答でエンコーディングが同一** → 収束判定が等値比較で済む
      - ✅ 実機の状態応答（`0x71`）で裏付け済み（C23-7）
- [x] ランダムアドレスでの参加が「登録済みコントローラ 4 台まで」の枠を消費するか
      → 10 個以上のアドレスを使ったが問題は出ていない。そもそも回転は不要になった（C32）
- [x] 器具が接続してくる条件 → **広告開始から約 5 秒**。
      同じアドレスでも古いリンクを切れば繋がる（C31-2 / C32）
- [x] 常夜灯の状態を器具から読む → ✅ **`0x71` 応答の `data[7]`**（0=消灯 / 1〜3、
      3 が最も明るい。コマンドとは逆順）。純正アプリはこのバイトを読んでいない（C24-5）
- [ ] `01 18 xx 00`（CMD 0x18）の意味（純正アプリも「Unknow CMD」として捨てている）
- [ ] `0x71` 応答の `data[8..12]`（実測は全て 0。サイド RGB の枠らしい）
- [ ] 器具ファームウェアの独自コンテナ（圧縮 or 差分パッチ）の解析
      → 暗号化はされていないが、そのままでは逆アセンブルできない。
      手元の器具（21184）向けのファイルは同梱されていない（C25）。
      チップは **Cypress CYW207xx 系（Cortex-M4）** と判明（C26-5）
- [ ] `Check Data Route[0x07]`（中継経路の取得）が電波側にも露出しているか
      → メッシュのトポロジ診断に使える（C26-3）
